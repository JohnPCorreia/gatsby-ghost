import { COLOR_SCHEMA } from "../../../../constants/styles/main"

const COLORS = {
  BORDER: `${COLOR_SCHEMA.UTILITY.GREY}`,
  SUMMARY: `${COLOR_SCHEMA.PRIMARY.DARK_GREY}`,
  DETAILS: `${COLOR_SCHEMA.PRIMARY.LIGHT_GREY}`,
}

export default COLORS
